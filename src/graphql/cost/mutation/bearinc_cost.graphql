mutation mutateBearingCostSet($bearingCost: [BearingCostInputList]) {
    mutateBearingCostSet: BearingCostSet(input: {
        bearingCostInputs:$bearingCost
    }) {
        clientMutationId
        isValid
    }
}
