mutation mutateCertificate(
    $id:ID,
    $name:String!
){
    mutateCertificate: certificateMutation(input:{
        id:$id,
        name:$name
    }) {
        clientMutationId
        errors{
            field
            messages
        }
    }
}