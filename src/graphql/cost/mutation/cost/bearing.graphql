mutation mutateBearing(
    $id:ID,
    $name:String!
){
    mutateBearing: bearingMutation(input:{
        id:$id,
        name:$name
    }) {
        clientMutationId
        errors{
            field
            messages
        }
    }
}